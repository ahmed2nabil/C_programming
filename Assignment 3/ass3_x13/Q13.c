/*
 * Q13.c
 *
 *  Created on: Jul 18, 2018
 *      Author: nobol billy
 */
#include<stdio.h>
#define size 100
int main(){
	setvbuf(stdout, NULL, _IONBF, 0);
	setvbuf(stderr, NULL, _IONBF, 0);
	int  c,i=0 ,j=0,y=0;
    char str[size];
	printf("enter string=");
    gets(str);



}
